# cubes-cpp
Cubes game

## Build
Download **SFML** package first:  
- Debian-based (Ubuntu, Linux Mint): `sudo apt install libsfml-dev`  
- CentOS, Fedora, etc: `sudo yum install SFML`

```bash
$ git clone https://github.com/imring/cubes-cpp
$ cd cubes-cpp && make
$ ./cubes
```